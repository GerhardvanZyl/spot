import { IContactInfoViewModel } from './icontact-info.viewmodel';

export class ContactInfoViewModel {
    phoneNumbers: string[];
    emailAddresses: string[];

    constructor(contactInfo: IContactInfoViewModel){
        
    }
}